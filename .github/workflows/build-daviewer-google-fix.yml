name: Build DAViewer popup OAuth fix

on:
  push:
    branches:
      - build/daviewer-google-oauth-fix
  workflow_dispatch:

permissions:
  contents: read

jobs:
  android:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Checkout DAViewer upstream
        uses: actions/checkout@v4
        with:
          repository: redtidev1918/daviewer
          ref: main
          path: daviewer

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.47.1
          channel: stable
          cache: true

      - name: Patch embedded Google OAuth popup handling
        shell: bash
        run: |
          cat > /tmp/oncreate.txt <<'EOF'
                    onCreateWindow: (controller, createWindowAction) async {
                      final windowId = createWindowAction.windowId;
                      if (mounted) {
                        setState(() => _popupWindowId = windowId);
                      }
                      return true;
                    },
          EOF
          cat > /tmp/popup.txt <<'EOF'
          if (_popupWindowId != null)
            Positioned.fill(
              child: Material(
                color: theme.scaffoldBackgroundColor,
                child: SafeArea(
                  child: Stack(
                    children: <Widget>[
                      InAppWebView(
                        windowId: _popupWindowId,
                        initialSettings: InAppWebViewSettings(
                          javaScriptEnabled: true,
                          userAgent: webUserAgent,
                          thirdPartyCookiesEnabled: true,
                          javaScriptCanOpenWindowsAutomatically: true,
                          supportMultipleWindows: false,
                        ),
                        shouldOverrideUrlLoading:
                            (popupController, navigationAction) async {
                          final uri = navigationAction.request.url;
                          if (uri != null &&
                              uri.scheme == 'dakit' &&
                              uri.host == 'oauth') {
                            _bridge?.addCallback(uri);
                            if (mounted) {
                              setState(() => _popupWindowId = null);
                            }
                            final parent = _controller;
                            if (parent != null) {
                              unawaited(
                                parent.loadUrl(
                                  urlRequest: URLRequest(
                                    url: WebUri(_homeUri.toString()),
                                  ),
                                ),
                              );
                            }
                            return NavigationActionPolicy.CANCEL;
                          }
                          return NavigationActionPolicy.ALLOW;
                        },
                        onCloseWindow: (popupController) {
                          if (mounted) {
                            setState(() => _popupWindowId = null);
                          }
                          final parent = _controller;
                          if (parent != null) {
                            unawaited(
                              parent.loadUrl(
                                urlRequest: URLRequest(
                                  url: WebUri(_homeUri.toString()),
                                ),
                              ),
                            );
                          }
                        },
                      ),
                      Positioned(
                        top: 4,
                        right: 4,
                        child: Material(
                          color: theme.colorScheme.surface.withValues(alpha: 0.9),
                          shape: const CircleBorder(),
                          child: IconButton(
                            icon: const Icon(Icons.close),
                            onPressed: () {
                              if (mounted) {
                                setState(() => _popupWindowId = null);
                              }
                            },
                          ),
                        ),
                      ),
                    ],
                  ),
                ),
              ),
            ),
          EOF
          python3 - <<'PY'
          from pathlib import Path

          p = Path("daviewer/lib/features/web_login/web_login_screen.dart")
          s = p.read_text()

          if "int? _popupWindowId;" not in s:
              anchor = "  InAppWebViewController? _controller;\n"
              if anchor not in s:
                  raise SystemExit("controller field patch point not found")
              s = s.replace(anchor, anchor + "  int? _popupWindowId;\n", 1)

          if "supportMultipleWindows: true," not in s:
              anchor = "                    initialSettings: InAppWebViewSettings(\n                      javaScriptEnabled: true,\n"
              repl = anchor + "                      supportMultipleWindows: true,\n                      javaScriptCanOpenWindowsAutomatically: true,\n                      thirdPartyCookiesEnabled: true,\n"
              if anchor not in s:
                  raise SystemExit("main settings patch point not found")
              s = s.replace(anchor, repl, 1)

          if "onCreateWindow: (controller, createWindowAction)" not in s:
              anchor = "                    shouldOverrideUrlLoading:\n"
              if anchor not in s:
                  raise SystemExit("navigation patch point not found")
              s = s.replace(anchor, Path("/tmp/oncreate.txt").read_text() + anchor, 1)

          if "windowId: _popupWindowId," not in s:
              anchor = "          if (_closeAfterReport) _LoginSuccessOverlay(s: s),\n"
              if anchor not in s:
                  raise SystemExit("overlay patch point not found")
              s = s.replace(anchor, Path("/tmp/popup.txt").read_text() + anchor, 1)

          p.write_text(s)

          router = Path("daviewer/lib/app/router.dart")
          rs = router.read_text()
          old = """bool isSignedOutRoute(String location) =>
    location == '/' ||
    location == '/watch' ||
    location == '/web-login' ||
    location == '/settings' ||
    location.startsWith('/settings/');
"""
          new = """bool isSignedOutRoute(String location) =>
    location == '/' ||
    location == '/watch' ||
    location == '/search' ||
    location == '/downloads' ||
    location == '/web-login' ||
    location == '/settings' ||
    location.startsWith('/settings/') ||
    location.startsWith('/artwork/') ||
    location.startsWith('/tag/');
"""
          if old not in rs:
              raise SystemExit("router signed-out route patch point not found")
          router.write_text(rs.replace(old, new, 1))
          print("Patched", p, "and", router)
          PY

      - name: Install dependencies
        shell: bash
        run: |
          cd daviewer
          for attempt in 1 2 3 4 5; do
            if flutter pub get; then exit 0; fi
            echo "flutter pub get failed (attempt $attempt/5), retrying..."
            sleep 15
          done
          exit 1

      - name: Create temporary CI signing config
        shell: bash
        run: |
          cd daviewer
          keytool -genkeypair -v \
            -keystore android/app/upload-keystore.p12 \
            -storetype PKCS12 \
            -storepass android \
            -keypass android \
            -alias upload \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -dname "CN=DAViewer Test Build, OU=CI, O=Local, L=Berlin, C=DE"
          cat > android/key.properties <<'EOF'
          storePassword=android
          keyPassword=android
          keyAlias=upload
          storeFile=upload-keystore.p12
          storeType=PKCS12
          EOF

      - name: Build installable debug APK
        shell: bash
        run: |
          cd daviewer
          dart format lib/features/web_login/web_login_screen.dart lib/app/router.dart
          flutter analyze lib/features/web_login/web_login_screen.dart lib/app/router.dart
          flutter build apk --debug
          cp build/app/outputs/flutter-apk/app-debug.apk ../DAViewer-popup-oauth-search-fix.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: DAViewer-popup-oauth-search-fix
          path: DAViewer-popup-oauth-search-fix.apk
          retention-days: 7
          if-no-files-found: error

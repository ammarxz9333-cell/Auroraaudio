[package]
name = "aurora-alsa-input"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
thiserror.workspace = true

[target.'cfg(target_os = "linux")'.dependencies]
# Keep one libasound wrapper generation across the workspace while CPAL remains
# on 0.15. A dedicated CPAL upgrade can move both together after the exact Linux
# branch is green.
alsa = "0.9"
